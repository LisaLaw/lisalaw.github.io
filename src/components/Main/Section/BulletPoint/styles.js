import styled from "styled-components";

const StyledBPContent = styled.div`
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  margin: 15px 0;
  border: 1px dashed red;
`;

export { StyledBPContent };
